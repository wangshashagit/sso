
systemInfo.appName = sso

spring:
    application:
        name: ${systemInfo.appName}
    freemarker:
        prefer-file-system-access: false
   #redis配置
    redis:
        host: 127.0.0.1
        port: 6379
        password: redis

  #数据源配置
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        type: com.alibaba.druid.pool.DruidDataSource
        url: jdbc:mysql://localhost:3306/user?useUnicode=true&amp&characterEncoding=UTF-8
        username: root
        password: wss
        druid:
            connection-properties: useUnicode=true;characterEncoding=UTF-8;allowMultiQueries=true;druid.stat.mergeSql=true
  #jpa
    jpa:
      show-sql: true
    session:
      store-type: redis

    server:
      port: 8080
      servlet:
          context-path: /auth